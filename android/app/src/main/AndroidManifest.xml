<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    
    <!-- 전화 걸기 권한 -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    
    <!-- 연락처 읽기 권한 -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    
    <!-- 인터넷 권한 (필요시) -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <application
        android:label="Quick Call"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <!-- 🎯 1×1 위젯 설정 Activity -->
        <activity
            android:name=".widget.WidgetConfigActivity1x1"
            android:exported="true"
            android:label="Quick Call 1×1 설정"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>
        
        <!-- 🎯 2×3 위젯 설정 Activity -->
        <activity
            android:name=".widget.WidgetConfigActivity2x3"
            android:exported="true"
            android:label="Quick Call 2×3 설정"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>
        
        <!-- 🎯 3×2 위젯 설정 Activity -->
        <activity
            android:name=".widget.WidgetConfigActivity3x2"
            android:exported="true"
            android:label="Quick Call 3×2 설정"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>
        
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        
        <!-- 🆕 1×1 위젯 Provider -->
        <receiver
            android:name=".widget.SpeedDialWidgetProvider1x1"
            android:exported="true"
            android:label="Quick Call 1×1">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.quick_call.ACTION_CALL_1X1" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/speed_dial_widget_info_1x1" />
        </receiver>
        
        <!-- 🆕 2×3 위젯 Provider -->
        <receiver
            android:name=".widget.SpeedDialWidgetProvider2x3"
            android:exported="true"
            android:label="Quick Call 2×3">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.quick_call.ACTION_CALL_2X3" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/speed_dial_widget_info_2x3" />
        </receiver>
        
        <!-- 🆕 3×2 위젯 Provider -->
        <receiver
            android:name=".widget.SpeedDialWidgetProvider3x2"
            android:exported="true"
            android:label="Quick Call 3×2">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.quick_call.ACTION_CALL_3X2" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/speed_dial_widget_info_3x2" />
        </receiver>
    </application>
    
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
        <intent>
            <action android:name="android.intent.action.CALL" />
            <data android:scheme="tel" />
        </intent>
    </queries>
</manifest>